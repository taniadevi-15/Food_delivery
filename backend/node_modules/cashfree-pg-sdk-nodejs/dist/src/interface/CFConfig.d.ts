export declare enum CFEnvironment {
    SANDBOX = "SANDBOX",
    PRODUCTION = "PRODUCTION"
}
export declare class CFConfig {
    environment: CFEnvironment;
    apiVersion: string;
    clientId: string;
    clientSecret: string;
    timeout: Number;
    webProxy: any;
    constructor(environment: CFEnvironment, apiVersion: string, clientId: string, clientSecret: string, timeout?: number, webProxy?: null);
}
